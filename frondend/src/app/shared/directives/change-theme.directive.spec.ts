import { ChangeThemeDirective } from './change-theme.directive';

describe('ChangeThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeThemeDirective();
    expect(directive).toBeTruthy();
  });
});
