import { ShortenPipe } from './shorten.pipe';

describe('ShortenPipe', () => {

  // const pipe = new ShortenPipe();
  // beforeEach(() => {
  //   p
  // });

  it('create an instance', () => {
    const pipe = new ShortenPipe();
    expect(pipe).toBeTruthy();
  });

  // it('transform "abcdef" to "abcdef"', () => {
  //   expect(pipe.transform('abcdef', 10)).toBe('abcdef');
  // });
});
